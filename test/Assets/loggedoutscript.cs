using System.Collections;
using System.Collections.Generic;
using UnityEngine;
public class loggedoutscript : MonoBehaviour
{
    // Start is called before the first frame update
    public AudioSource congrats;

    void Start()
    {
        congrats.Play();
    }

    void Update()
    {
        
    }
}
